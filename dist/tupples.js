function doSomething(pair) {
    var a = pair[0];
    var b = pair[1];
    var destructedString = pair[0], destructedNumber = pair[1];
    console.log(a, b, destructedNumber, destructedString);
}
function strNum(p) {
    var a = p[0];
}
